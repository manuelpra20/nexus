import { Injectable } from '@angular/core';
import { Observable, of } from "rxjs";
import { images } from "../../inter";

@Injectable({
  providedIn: 'root'
})
export class ImageService {

  constructor() { }
}
